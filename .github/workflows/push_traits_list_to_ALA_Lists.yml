name: Push CSV to ALA Lists

on:
  push:
    branches:
      - main
    paths:
      - 'imported_GoogleSheets/*.csv'  # triggers only when CSV files are changed

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Show file that triggered this
        run: echo "CSV file pushed — ready to trigger the ALA upload pipeline."
